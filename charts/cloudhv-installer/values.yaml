image:
  repository: ghcr.io/devigned/cloudhv-installer
  tag: ""  # Defaults to appVersion
  pullPolicy: IfNotPresent

imagePullSecrets: []
# For dev images from a private GHCR repo:
#   imagePullSecrets:
#     - name: ghcr-secret

nodeSelector:
  workload: cloudhv

tolerations:
  - operator: "Exists"

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "1"
    memory: "1Gi"

runtimeClass:
  enabled: true
  name: cloudhv
  handler: cloudhv
  overhead:
    memory: "50Mi"
    cpu: "10m"
